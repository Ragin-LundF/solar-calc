// Runtime config. Edit this file on the server after deploy — no rebuild needed.
// apiBase: where the backend lives. Same-origin default is '/api/v1'.
// For a backend on another port/host, use an absolute URL, e.g.:
//   window.__env = { apiBase: 'http://localhost:8080/api/v1' };
window.__env = { apiBase: '/api/v1' };